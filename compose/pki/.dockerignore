# 受領した証明書 / 秘密鍵をイメージのビルドコンテキストへ送らない。
# provided/ は実行時に bind mount するだけで、イメージには一切含めない。
provided/

# 出力先も同じ。export/ は pki-init が実行時に書き出す場所であって、
# イメージへ含めるものではない (ビルドへ渡すときは build secret を使う)。
# 証明書と CA 秘密鍵が入るため、ビルドコンテキストへも送らない。
export/
